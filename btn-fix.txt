/* Secondary add button (redundant, always visible below form) */
      .btn-add-secondary {
        margin-top: 1rem;
        padding: 1rem 2rem;
        background: var(--accent-primary);
        color: white;
        border: none;
        border-radius: 8px;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        display: block;
        width: 100%;
        max-width: 300px;
      }
      
      .btn-add-secondary:hover {
        background: var(--accent-hover);
        transform: translateY(-2px);
      }
