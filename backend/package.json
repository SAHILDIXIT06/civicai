{
  "name": "civic-ai-backend",
  "version": "0.1.0",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "predev": "kill-port 4000 || echo Port cleanup skipped",
    "dev": "nodemon src/server.js",
    "prestart": "kill-port 4000 || echo Port cleanup skipped",
    "start": "node src/server.js",
    "clean": "kill-port 4000 || echo Port cleanup skipped"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.13.1",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "nodemon": "^3.1.4",
    "npm-run-all": "^4.1.5"
  }
}
